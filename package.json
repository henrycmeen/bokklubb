{
    "name": "my-netlify-site",
    "version": "1.0.0",
    "scripts": {
        "build": "echo 'No build step' && exit 0"
    },
    "dependencies": {
        "node-fetch": "^2.7.0"
    },
    "devDependencies": {
        "netlify-cli": "^18.0.3"
    }
}
